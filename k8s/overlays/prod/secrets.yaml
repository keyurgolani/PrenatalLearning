# IMPORTANT: This is a placeholder! 
# In production, use sealed-secrets, external-secrets, or your cloud provider's secret management
# DO NOT commit real secrets to version control!
apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
  namespace: prenatal-learning-prod
type: Opaque
stringData:
  username: admin
  # Replace with a strong password or use external secret management
  password: REPLACE_WITH_SECURE_PASSWORD
---
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: prenatal-learning-prod
type: Opaque
stringData:
  # Replace with a secure JWT secret or use external secret management
  jwt-secret: REPLACE_WITH_SECURE_JWT_SECRET
